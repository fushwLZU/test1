#include <iostream>
#include <string>
#include <algorithm> 
using namespace std;
/*
void reverse(const string& a);

int main()
{
    string str;
    cout << " Please enter a string " << endl;
    getline(cin, str);
    reverse(str);
    return 0;
}
void reverse(const string& a)
{
    string s = a;
    int len = a.size();
    for (int i = len - 1; i >= 0; i--)
    {
        s[len - i - 1] = a[i];
    }
    s[len] = '\0';
    cout << s << endl;
}
*/

/*
#include "Complex.h"
#include <iostream>
using namespace std;

int main() {
    complex left(1, 2), right(3, 4);

    complex result = left + right;
    complex result1 = left - right;
    complex result2 = left * right;
    complex result3 = left / right;
    cout << result<<endl;
    cout << result1<<endl;
    cout << result2 << endl;
    cout << result3 << endl;
    cin >> left;
    cout << left << endl;

    return 0;
}
*/

